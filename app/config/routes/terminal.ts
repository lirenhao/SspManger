import { IRoute } from 'umi';

const routes: IRoute[] = [
  {
    path: '/terminal/info',
    name: 'terminal info',
    component: './terminal/info',
    locale: 'terminal.info',
  },
];

export default routes;
